{% extends 'base.html.twig' %}

{% block title %}Rubrique index{% endblock %}

{% block body %}
    <h1>Rubrique index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdRubrique</th>
                <th>NomRubrique</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rubrique in rubriques %}
            <tr>
                <td>{{ rubrique.idRubrique }}</td>
                <td>{{ rubrique.nomRubrique }}</td>
                <td>
                    <a href="{{ path('rubrique_show', {'idRubrique': rubrique.idRubrique}) }}">show</a>
                    <a href="{{ path('rubrique_edit', {'idRubrique': rubrique.idRubrique}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('rubrique_new') }}">Create new</a>
{% endblock %}
